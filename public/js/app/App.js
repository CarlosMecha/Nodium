define(['jquery', 'app/routers/Router'], function ($, Router) {
    var router = new Router(),
        initialize = function () {
        };
    return {
        initialize: initialize,
        router: router
    };
});
